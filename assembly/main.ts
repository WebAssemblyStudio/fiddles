declare function sayHello(): void;

sayHello();

export function add(x: i32, y: i32): i32 {
  return x + y;
}
export function compass (f:Float32Array) : f32 {
  // let len = f.length;
  // let xx = new Uint16Array(1);
  // xx[0] = 0x00FF;
  //  let xx = new Float32Array(1); 
  //  xx[0] =f;
  return  f[0];
}