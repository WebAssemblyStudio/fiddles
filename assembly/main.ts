declare function sayHello(): void;

var left = "key1";
var right = "key2";


assert(left == right);

sayHello();

export function add(x: i32, y: i32): i32 {
  return x + y;
}

export function compare(x: string, y: string): bool {
  return (x == y);
}

